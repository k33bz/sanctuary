# gmc101 lives on ls2087. The CD key for this host is org variable
# OP_REF_CD_LS2087; the job loads it into a transient ssh-agent, never to disk.
[gmc]
192.168.11.120 ansible_user=claude ansible_python_interpreter=/usr/bin/python3
